import { defineConfig } from 'astro/config';

export default defineConfig({
  // L'adresse complète de votre site GitHub Pages
  site: 'https://ouchinou.github.io',
  // Le nom exact de votre dépôt GitHub précédé et suivi d'un slash
  base: '/methode-musique/',
});